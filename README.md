# Project for MongoDB@FMI

Install dependencies:

```
npm install
```

Build clientside js:

```
watchify chat.js -o public/js/bundle.js -v
```

Start application server:

```
node app.js
```

Open `localhost:3000`.
